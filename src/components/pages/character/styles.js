import { StyleSheet } from 'react-native';

// Declaración de estilos para nuestra lista principal de personajes
const containerScreen = StyleSheet.create({
  container: {
    flex: 1,
    backgroundColor: 'black',
  },
});

export default containerScreen;
